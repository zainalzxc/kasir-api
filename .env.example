# Database Configuration
# Untuk local development (ganti dengan kredensial PostgreSQL Anda)
# DATABASE_URL=host=localhost user=postgres password=postgres dbname=kasir_db port=5432 sslmode=disable

# Untuk production (Railway/Supabase)
# DATABASE_URL akan di-set otomatis oleh Railway atau bisa dari Supabase

# Redis Configuration (Optional - untuk caching)
# Untuk local development
# REDIS_URL=redis://localhost:6379/0

# Untuk production (Railway/Upstash)
# REDIS_URL akan di-set otomatis oleh Railway atau dari Upstash
# Format: redis://username:password@host:port/db

# Port (opsional, default 8080)
# PORT=8080
